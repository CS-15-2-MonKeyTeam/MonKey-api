# import User from "./generated/prisma.graphql"

type Query {
  me: User
}

type Mutation {
  auth(accountkitAccessToken: String!, name: String): AuthPayload!
}

type AuthPayload {
  token: String!
  user: User!
}
